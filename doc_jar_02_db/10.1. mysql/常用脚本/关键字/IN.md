<span  style="font-family: Simsun,serif; font-size: 17px; ">

[TOC]

### IN

~~~sql
SELECT 1 = 2;
SELECT 1 = 1;
-- NULL 代表未知的值，它与任何值（包括另一个 NULL 值）进行比较的结果都是 UNKNOWN，而非 TRUE 或 FALSE。
-- NOT IN 操作本质上是一系列的不等于比较。
SELECT 1 = null;
~~~

### NOT IN

</span>