<span style="font-family:Simsun,serif; font-size:17px;">

[TOC]

### 非阻塞 I/O

- NIO（Non-blocking I/O）?是一种非阻塞式IO模型。
- 多线程切换有一定的开销，因此引入非阻塞 I/O。
- 非阻塞 I/O 不会将进程挂起，调用时会立即返回成功或错误，因此可以在一个线程里轮询多个文件描述符是否就绪。
- 但是非阻塞 I/O 的缺点是：每次发起系统调用，只能检查一个文件描述符是否就绪。
    - 当文件描述符很多时，系统调用的成本很高。

</span>