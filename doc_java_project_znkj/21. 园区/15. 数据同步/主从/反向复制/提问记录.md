
### 

~~~
Error connecting to source 'repl_user@192.168.0.8:3305'. This was attempt 3/86400, with a delay of 60 seconds between attempts. Message: Authentication plugin 'caching_sha2_password' reported error: Authentication
~~~


###

~~~
Got fatal error 1236 from source when reading data from binary log: 'Could not find first log file name in binary log index file'
~~~


###

~~~
mysql -u root -prootpass -e "SHOW SLAVE STATUS FOR CHANNEL 'slave1'\G"

~~~

### 跳过事务

~~~
-- 停止复制通道
STOP SLAVE FOR CHANNEL 'slave1';

-- 跳过当前事务
SET GLOBAL sql_slave_skip_counter = 1;

-- 启动复制
START SLAVE FOR CHANNEL 'slave1';

~~~
~~~
-- 停止复制通道
STOP SLAVE FOR CHANNEL 'slave2';

-- 跳过当前事务
SET GLOBAL sql_slave_skip_counter = 1;

-- 启动复制
START SLAVE FOR CHANNEL 'slave2';

~~~


~~~
-- 停止并重置复制通道
STOP SLAVE FOR CHANNEL 'slave1';
RESET SLAVE FOR CHANNEL 'slave1';

-- 使用主库当前 binlog 位置
CHANGE MASTER TO
  MASTER_HOST='192.168.0.8',
  MASTER_PORT=3307,
  MASTER_USER='repl_user',
  MASTER_PASSWORD='repl_pass'
FOR CHANNEL 'slave1';

-- 启动复制
START SLAVE FOR CHANNEL 'slave1';

~~~

~~~
-- 停止并重置复制通道
STOP SLAVE FOR CHANNEL 'slave2';
RESET SLAVE FOR CHANNEL 'slave2';

-- 使用主库当前 binlog 位置
CHANGE MASTER TO
  MASTER_HOST='192.168.0.8',
  MASTER_PORT=3308,
  MASTER_USER='repl_user',
  MASTER_PASSWORD='repl_pass'
FOR CHANNEL 'slave2';

-- 启动复制
START SLAVE FOR CHANNEL 'slave2';

~~~

