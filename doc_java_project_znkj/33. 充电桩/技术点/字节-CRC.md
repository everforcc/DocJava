<span  style="font-family: Simsun,serif; font-size: 17px; ">

### 0X01

- 一个字节
- 0x01 是一个十六进制数，表示的是 1个字节。
- 0x01 的值为十进制的 1
- 十六进制表示法中，每个两位十六进制数代表一个字节（8位）
- 所以 0x01 占用 1 个字节的存储空间
- 在计算机中，1个字节可以表示 0-255 之间的数值（无符号），或 -128 到 127 之间的数值（有符号）。

### CRC16

- 校验算法

</span>