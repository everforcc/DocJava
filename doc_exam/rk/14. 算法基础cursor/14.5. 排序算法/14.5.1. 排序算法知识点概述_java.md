# 14.5.1. 排序算法知识点概述 - Java代码示例

## 简单排序算法

```java
/**
 * 简单排序算法
 */
public class SimpleSorting {
    
    /**
     * 冒泡排序
     */
    public static void bubbleSort(int[] arr) {
        int n = arr.length;
        for (int i = 0; i < n - 1; i++) {
            boolean swapped = false;
            for (int j = 0; j < n - i - 1; j++) {
                if (arr[j] > arr[j + 1]) {
                    swap(arr, j, j + 1);
                    swapped = true;
                }
            }
            // 如果没有发生交换，说明数组已经有序
            if (!swapped) {
                break;
            }
        }
    }
    
    /**
     * 选择排序
     */
    public static void selectionSort(int[] arr) {
        int n = arr.length;
        for (int i = 0; i < n - 1; i++) {
            int minIndex = i;
            for (int j = i + 1; j < n; j++) {
                if (arr[j] < arr[minIndex]) {
                    minIndex = j;
                }
            }
            if (minIndex != i) {
                swap(arr, i, minIndex);
            }
        }
    }
    
    /**
     * 插入排序
     */
    public static void insertionSort(int[] arr) {
        int n = arr.length;
        for (int i = 1; i < n; i++) {
            int key = arr[i];
            int j = i - 1;
            
            // 将大于key的元素向后移动
            while (j >= 0 && arr[j] > key) {
                arr[j + 1] = arr[j];
                j--;
            }
            arr[j + 1] = key;
        }
    }
    
    /**
     * 希尔排序（插入排序的改进）
     */
    public static void shellSort(int[] arr) {
        int n = arr.length;
        
        // 使用Knuth序列
        int gap = 1;
        while (gap < n / 3) {
            gap = gap * 3 + 1;
        }
        
        while (gap > 0) {
            for (int i = gap; i < n; i++) {
                int key = arr[i];
                int j = i;
                
                while (j >= gap && arr[j - gap] > key) {
                    arr[j] = arr[j - gap];
                    j -= gap;
                }
                arr[j] = key;
            }
            gap /= 3;
        }
    }
    
    /**
     * 交换两个元素
     */
    private static void swap(int[] arr, int i, int j) {
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
```

## 高级排序算法

```java
/**
 * 高级排序算法
 */
public class AdvancedSorting {
    
    /**
     * 快速排序
     */
    public static void quickSort(int[] arr) {
        quickSort(arr, 0, arr.length - 1);
    }
    
    private static void quickSort(int[] arr, int low, int high) {
        if (low < high) {
            int pivotIndex = partition(arr, low, high);
            quickSort(arr, low, pivotIndex - 1);
            quickSort(arr, pivotIndex + 1, high);
        }
    }
    
    private static int partition(int[] arr, int low, int high) {
        // 三数取中法选择基准
        int mid = low + (high - low) / 2;
        if (arr[mid] < arr[low]) swap(arr, low, mid);
        if (arr[high] < arr[low]) swap(arr, low, high);
        if (arr[high] < arr[mid]) swap(arr, mid, high);
        
        int pivot = arr[mid];
        swap(arr, mid, high);
        
        int i = low - 1;
        for (int j = low; j < high; j++) {
            if (arr[j] <= pivot) {
                i++;
                swap(arr, i, j);
            }
        }
        swap(arr, i + 1, high);
        return i + 1;
    }
    
    /**
     * 归并排序
     */
    public static void mergeSort(int[] arr) {
        mergeSort(arr, 0, arr.length - 1);
    }
    
    private static void mergeSort(int[] arr, int left, int right) {
        if (left < right) {
            int mid = left + (right - left) / 2;
            mergeSort(arr, left, mid);
            mergeSort(arr, mid + 1, right);
            merge(arr, left, mid, right);
        }
    }
    
    private static void merge(int[] arr, int left, int mid, int right) {
        int n1 = mid - left + 1;
        int n2 = right - mid;
        
        int[] leftArr = new int[n1];
        int[] rightArr = new int[n2];
        
        System.arraycopy(arr, left, leftArr, 0, n1);
        System.arraycopy(arr, mid + 1, rightArr, 0, n2);
        
        int i = 0, j = 0, k = left;
        
        while (i < n1 && j < n2) {
            if (leftArr[i] <= rightArr[j]) {
                arr[k++] = leftArr[i++];
            } else {
                arr[k++] = rightArr[j++];
            }
        }
        
        while (i < n1) {
            arr[k++] = leftArr[i++];
        }
        
        while (j < n2) {
            arr[k++] = rightArr[j++];
        }
    }
    
    /**
     * 堆排序
     */
    public static void heapSort(int[] arr) {
        int n = arr.length;
        
        // 构建最大堆
        for (int i = n / 2 - 1; i >= 0; i--) {
            heapify(arr, n, i);
        }
        
        // 逐个取出堆顶元素
        for (int i = n - 1; i > 0; i--) {
            swap(arr, 0, i);
            heapify(arr, i, 0);
        }
    }
    
    private static void heapify(int[] arr, int n, int i) {
        int largest = i;
        int left = 2 * i + 1;
        int right = 2 * i + 2;
        
        if (left < n && arr[left] > arr[largest]) {
            largest = left;
        }
        
        if (right < n && arr[right] > arr[largest]) {
            largest = right;
        }
        
        if (largest != i) {
            swap(arr, i, largest);
            heapify(arr, n, largest);
        }
    }
    
    private static void swap(int[] arr, int i, int j) {
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
```

## 线性时间排序算法

```java
/**
 * 线性时间排序算法
 */
public class LinearTimeSorting {
    
    /**
     * 计数排序
     */
    public static void countingSort(int[] arr) {
        if (arr.length == 0) return;
        
        // 找到最大值和最小值
        int max = arr[0], min = arr[0];
        for (int num : arr) {
            max = Math.max(max, num);
            min = Math.min(min, num);
        }
        
        int range = max - min + 1;
        int[] count = new int[range];
        
        // 统计每个值的出现次数
        for (int num : arr) {
            count[num - min]++;
        }
        
        // 根据统计结果重新排列数组
        int index = 0;
        for (int i = 0; i < range; i++) {
            while (count[i] > 0) {
                arr[index++] = i + min;
                count[i]--;
            }
        }
    }
    
    /**
     * 基数排序
     */
    public static void radixSort(int[] arr) {
        if (arr.length == 0) return;
        
        // 找到最大值
        int max = arr[0];
        for (int num : arr) {
            max = Math.max(max, num);
        }
        
        // 从个位开始，对每一位进行计数排序
        for (int exp = 1; max / exp > 0; exp *= 10) {
            countingSortByDigit(arr, exp);
        }
    }
    
    private static void countingSortByDigit(int[] arr, int exp) {
        int n = arr.length;
        int[] output = new int[n];
        int[] count = new int[10];
        
        // 统计每个数字的出现次数
        for (int num : arr) {
            count[(num / exp) % 10]++;
        }
        
        // 计算累积计数
        for (int i = 1; i < 10; i++) {
            count[i] += count[i - 1];
        }
        
        // 构建输出数组
        for (int i = n - 1; i >= 0; i--) {
            output[count[(arr[i] / exp) % 10] - 1] = arr[i];
            count[(arr[i] / exp) % 10]--;
        }
        
        // 复制回原数组
        System.arraycopy(output, 0, arr, 0, n);
    }
    
    /**
     * 桶排序
     */
    public static void bucketSort(double[] arr) {
        if (arr.length == 0) return;
        
        int n = arr.length;
        List<Double>[] buckets = new List[n];
        
        // 初始化桶
        for (int i = 0; i < n; i++) {
            buckets[i] = new ArrayList<>();
        }
        
        // 将元素分配到桶中
        for (double num : arr) {
            int bucketIndex = (int) (n * num);
            if (bucketIndex == n) bucketIndex--;
            buckets[bucketIndex].add(num);
        }
        
        // 对每个桶进行排序
        for (List<Double> bucket : buckets) {
            Collections.sort(bucket);
        }
        
        // 合并所有桶
        int index = 0;
        for (List<Double> bucket : buckets) {
            for (double num : bucket) {
                arr[index++] = num;
            }
        }
    }
}
```

## 排序算法工具类

```java
/**
 * 排序算法工具类
 */
public class SortingUtils {
    
    /**
     * 检查数组是否已排序
     */
    public static boolean isSorted(int[] arr) {
        for (int i = 1; i < arr.length; i++) {
            if (arr[i] < arr[i - 1]) {
                return false;
            }
        }
        return true;
    }
    
    /**
     * 生成随机数组
     */
    public static int[] generateRandomArray(int size, int max) {
        int[] arr = new int[size];
        Random random = new Random();
        for (int i = 0; i < size; i++) {
            arr[i] = random.nextInt(max);
        }
        return arr;
    }
    
    /**
     * 生成有序数组
     */
    public static int[] generateSortedArray(int size) {
        int[] arr = new int[size];
        for (int i = 0; i < size; i++) {
            arr[i] = i;
        }
        return arr;
    }
    
    /**
     * 生成逆序数组
     */
    public static int[] generateReverseArray(int size) {
        int[] arr = new int[size];
        for (int i = 0; i < size; i++) {
            arr[i] = size - i - 1;
        }
        return arr;
    }
    
    /**
     * 复制数组
     */
    public static int[] copyArray(int[] arr) {
        return Arrays.copyOf(arr, arr.length);
    }
    
    /**
     * 打印数组
     */
    public static void printArray(int[] arr) {
        System.out.println(Arrays.toString(arr));
    }
    
    /**
     * 测试排序算法性能
     */
    public static void testSortingPerformance(String algorithmName, 
                                            Consumer<int[]> sortFunction, 
                                            int[] arr) {
        int[] testArr = copyArray(arr);
        long startTime = System.nanoTime();
        sortFunction.accept(testArr);
        long endTime = System.nanoTime();
        
        System.out.printf("%s: %d ns, 排序结果: %s%n", 
            algorithmName, 
            endTime - startTime, 
            isSorted(testArr) ? "正确" : "错误");
    }
}
```

## 排序算法性能测试

```java
/**
 * 排序算法性能测试
 */
public class SortingPerformanceTest {
    
    public static void main(String[] args) {
        System.out.println("=== 排序算法性能测试 ===");
        testSortingPerformance();
        
        System.out.println("\n=== 不同数据规模测试 ===");
        testDifferentSizes();
        
        System.out.println("\n=== 不同数据特征测试 ===");
        testDifferentDataTypes();
    }
    
    /**
     * 测试排序算法性能
     */
    public static void testSortingPerformance() {
        int[] arr = SortingUtils.generateRandomArray(10000, 10000);
        
        System.out.println("测试规模: 10000个随机数");
        
        SortingUtils.testSortingPerformance("冒泡排序", SimpleSorting::bubbleSort, arr);
        SortingUtils.testSortingPerformance("选择排序", SimpleSorting::selectionSort, arr);
        SortingUtils.testSortingPerformance("插入排序", SimpleSorting::insertionSort, arr);
        SortingUtils.testSortingPerformance("希尔排序", SimpleSorting::shellSort, arr);
        SortingUtils.testSortingPerformance("快速排序", AdvancedSorting::quickSort, arr);
        SortingUtils.testSortingPerformance("归并排序", AdvancedSorting::mergeSort, arr);
        SortingUtils.testSortingPerformance("堆排序", AdvancedSorting::heapSort, arr);
        SortingUtils.testSortingPerformance("计数排序", LinearTimeSorting::countingSort, arr);
        SortingUtils.testSortingPerformance("基数排序", LinearTimeSorting::radixSort, arr);
    }
    
    /**
     * 测试不同数据规模
     */
    public static void testDifferentSizes() {
        int[] sizes = {1000, 5000, 10000, 50000};
        
        for (int size : sizes) {
            System.out.println("\n测试规模: " + size);
            int[] arr = SortingUtils.generateRandomArray(size, size);
            
            SortingUtils.testSortingPerformance("快速排序", AdvancedSorting::quickSort, arr);
            SortingUtils.testSortingPerformance("归并排序", AdvancedSorting::mergeSort, arr);
            SortingUtils.testSortingPerformance("堆排序", AdvancedSorting::heapSort, arr);
        }
    }
    
    /**
     * 测试不同数据特征
     */
    public static void testDifferentDataTypes() {
        int size = 10000;
        
        // 随机数据
        System.out.println("\n随机数据:");
        int[] randomArr = SortingUtils.generateRandomArray(size, size);
        SortingUtils.testSortingPerformance("快速排序", AdvancedSorting::quickSort, randomArr);
        SortingUtils.testSortingPerformance("归并排序", AdvancedSorting::mergeSort, randomArr);
        
        // 有序数据
        System.out.println("\n有序数据:");
        int[] sortedArr = SortingUtils.generateSortedArray(size);
        SortingUtils.testSortingPerformance("快速排序", AdvancedSorting::quickSort, sortedArr);
        SortingUtils.testSortingPerformance("归并排序", AdvancedSorting::mergeSort, sortedArr);
        
        // 逆序数据
        System.out.println("\n逆序数据:");
        int[] reverseArr = SortingUtils.generateReverseArray(size);
        SortingUtils.testSortingPerformance("快速排序", AdvancedSorting::quickSort, reverseArr);
        SortingUtils.testSortingPerformance("归并排序", AdvancedSorting::mergeSort, reverseArr);
    }
}
```

## 排序算法稳定性测试

```java
/**
 * 排序算法稳定性测试
 */
public class SortingStabilityTest {
    
    /**
     * 测试排序算法的稳定性
     */
    public static void testStability() {
        System.out.println("=== 排序算法稳定性测试 ===");
        
        // 创建包含重复元素的数组
        int[] arr = {3, 1, 4, 1, 5, 9, 2, 6, 5, 3};
        System.out.println("原数组: " + Arrays.toString(arr));
        
        // 测试各种排序算法
        testStability("冒泡排序", SimpleSorting::bubbleSort, copyArray(arr));
        testStability("选择排序", SimpleSorting::selectionSort, copyArray(arr));
        testStability("插入排序", SimpleSorting::insertionSort, copyArray(arr));
        testStability("快速排序", AdvancedSorting::quickSort, copyArray(arr));
        testStability("归并排序", AdvancedSorting::mergeSort, copyArray(arr));
        testStability("堆排序", AdvancedSorting::heapSort, copyArray(arr));
        testStability("计数排序", LinearTimeSorting::countingSort, copyArray(arr));
        testStability("基数排序", LinearTimeSorting::radixSort, copyArray(arr));
    }
    
    private static void testStability(String algorithmName, Consumer<int[]> sortFunction, int[] arr) {
        sortFunction.accept(arr);
        System.out.println(algorithmName + ": " + Arrays.toString(arr));
    }
    
    private static int[] copyArray(int[] arr) {
        return Arrays.copyOf(arr, arr.length);
    }
}
```

## 测试代码

```java
/**
 * 排序算法测试类
 */
public class SortingAlgorithmTest {
    public static void main(String[] args) {
        System.out.println("=== 排序算法测试 ===");
        testBasicSorting();
        
        System.out.println("\n=== 排序算法稳定性测试 ===");
        SortingStabilityTest.testStability();
        
        System.out.println("\n=== 排序算法性能测试 ===");
        SortingPerformanceTest.main(args);
    }
    
    /**
     * 测试基本排序算法
     */
    public static void testBasicSorting() {
        int[] arr = {64, 34, 25, 12, 22, 11, 90};
        System.out.println("原数组: " + Arrays.toString(arr));
        
        // 测试简单排序算法
        int[] bubbleArr = Arrays.copyOf(arr, arr.length);
        SimpleSorting.bubbleSort(bubbleArr);
        System.out.println("冒泡排序: " + Arrays.toString(bubbleArr));
        
        int[] selectionArr = Arrays.copyOf(arr, arr.length);
        SimpleSorting.selectionSort(selectionArr);
        System.out.println("选择排序: " + Arrays.toString(selectionArr));
        
        int[] insertionArr = Arrays.copyOf(arr, arr.length);
        SimpleSorting.insertionSort(insertionArr);
        System.out.println("插入排序: " + Arrays.toString(insertionArr));
        
        int[] shellArr = Arrays.copyOf(arr, arr.length);
        SimpleSorting.shellSort(shellArr);
        System.out.println("希尔排序: " + Arrays.toString(shellArr));
        
        // 测试高级排序算法
        int[] quickArr = Arrays.copyOf(arr, arr.length);
        AdvancedSorting.quickSort(quickArr);
        System.out.println("快速排序: " + Arrays.toString(quickArr));
        
        int[] mergeArr = Arrays.copyOf(arr, arr.length);
        AdvancedSorting.mergeSort(mergeArr);
        System.out.println("归并排序: " + Arrays.toString(mergeArr));
        
        int[] heapArr = Arrays.copyOf(arr, arr.length);
        AdvancedSorting.heapSort(heapArr);
        System.out.println("堆排序: " + Arrays.toString(heapArr));
        
        // 测试线性时间排序算法
        int[] countingArr = Arrays.copyOf(arr, arr.length);
        LinearTimeSorting.countingSort(countingArr);
        System.out.println("计数排序: " + Arrays.toString(countingArr));
        
        int[] radixArr = Arrays.copyOf(arr, arr.length);
        LinearTimeSorting.radixSort(radixArr);
        System.out.println("基数排序: " + Arrays.toString(radixArr));
        
        // 测试桶排序
        double[] bucketArr = {0.897, 0.565, 0.656, 0.1234, 0.665, 0.3434};
        System.out.println("桶排序前: " + Arrays.toString(bucketArr));
        LinearTimeSorting.bucketSort(bucketArr);
        System.out.println("桶排序后: " + Arrays.toString(bucketArr));
    }
}
```
