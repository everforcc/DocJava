<span  style="font-family: Simsun,serif; font-size: 17px; ">

[TOC]

### 计算机结构

```
-------------------------------------------
|         |---------------------|          (外设)|
| 输入设备 | 辅助存储器(辅存/外存)  |  输出设备  |
|         |        存储器        |          |
-------------------------------------------
|         |       主存储器       |          |
|         |---------------------|          |
|                                          |
|         |---------------------|          |
|         | 运算器        控制器  |          |
|         |        CPU          |          |
|         |---------------------|           (主机)|
-------------------------------------------
```

1. 运算器
2. 控制器

~~~mermaid
graph TB
输入设备 --> 主存储器
辅助存储器 --> 主存储器
主存储器 --> 输出设备
主存储器 --> 运算器
运算器 --> 主存储器
控制器 -.-> 运算器
控制器 -.-> 主存储器
控制器 -.-> 输入设备
控制器 -.-> 辅助存储器
控制器 -.-> 输出设备
~~~

### 2.CPU

#### 2.1 运算器

- 算术逻辑单元ALU
    - Arithmetic Logic Unit
    - 数据的算术运算和逻辑运算
- 累加寄存器AC
    - Accumulator
    - 通用寄存器，为ALU提供一个工作区，用在暂存数据
- 数据缓冲寄存器DR
    - Data Register
    - 写内存时，暂存指令或数据
- 状态条件寄存器PSW
    - Program Status Word
    - 存状态标志与控制标志
    - 有争议,也有将其归为控制器的

#### 2.2 控制器

- 程序计数器PC(地址)
    - Program Counter
    - 存储下一条要执行的指令的地址
- 指令寄存器IR(01 2进制)
    - Instruction Register
    - 存储即将执行的指令
- 指令译码器ID
    - Instruction Decoder
    - 对指令中的操作码字段进行分析解释
- 时序部件
    - 提供时序控制信号

</span>